using UnityEngine;

namespace Managers
{
    public class LocalizationManager : MonoBehaviour
    {
        public bool SetLocalization(string languageCode)
        {
            return false;
        }
    }
}